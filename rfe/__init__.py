"""Rational Field Equation engine.

One equation. All observables.

    N(p/q) = N_total * g(p/q) * w(p/q, K0 * F[N])

Solve the self-consistency condition on the Stern-Brocot tree,
extract physics from the fixed point.
"""

__version__ = "0.1.0"
